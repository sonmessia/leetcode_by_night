struct Solution;

impl Solution {
    pub fn min_operations(nums: Vec<i32>) -> i32 {
        let mut s: Vec<i32> = Vec::new();
        let mut ans = 0;

        for num in nums {}
        ans
    }
}
